import 'package:flutter/material.dart';

class SignInValidationPage extends StatelessWidget {
  const SignInValidationPage({Key? key, required this.phoneNumber}) : super(key: key);

  final String phoneNumber;

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.red,
    );
  }
}
